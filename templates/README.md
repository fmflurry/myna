# templates

JSON summary templates: prompt template + section schema + placeholders (`{transcript}`, `{duration}`, `{title}`). Built-ins will be `key-points`, `action-items`, `meeting-notes`, `decisions`; user-extensible per docs/stack-proposal.md. No templates yet.
